# Localhost Start

Go to:

```text
C:\Users\PP\OneDrive - PP Konsulting\Dokument\TrueKind\web
```

Run:

```powershell
npm.cmd run dev:local
```

Open:

```text
http://127.0.0.1:3000/discover
```

Keep the terminal running while the app is open.

Stop the server with:

```text
Ctrl + C
```
